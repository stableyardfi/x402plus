{
  "name": "facilitator-sample",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Sample facilitator implementing /vault, /balance, /verify, /settle for x402plus (exact + intent).",
  "main": "dist/server.js",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "start": "node dist/server.js",
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@aptos-labs/ts-sdk": "^1.33.1",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "ethers": "^6.16.0",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.21",
    "tsx": "^4.19.1",
    "typescript": "^5.6.3"
  }
}
